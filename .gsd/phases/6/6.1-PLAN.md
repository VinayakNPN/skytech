---
phase: 6
plan: 1
wave: 1
---

# Plan 6.1: Backend Leave API & Email Notifications

## Objective
Implement role-based leave routing, pending approval endpoints, and email notification infrastructure as per R6 and R7.

## Context
- .gsd/SPEC.md
- .gsd/ARCHITECTURE.md
- Backend/src/routes/employeeManagement.ts
- Backend/src/utils/leaveRouting.ts (to create)
- Backend/src/utils/emailService.ts (to create)

## Tasks

<task type="auto">
  <name>Create Leave Routing & Email Service</name>
  <files>
    Backend/src/utils/leaveRouting.ts
    Backend/src/utils/emailService.ts
  </files>
  <action>
    - Create `leaveRouting.ts` exporting a function `determineLeaveRoutingRole(applicantRole: string): string` based on R7 hierarchy.
    - Create `emailService.ts` to stub out or implement `sendLeaveApplicationEmail` and `sendLeaveStatusEmail` using `nodemailer`. Ensure it handles missing environment variables gracefully (log to console if SMTP is not configured).
  </action>
  <verify>tsc --noEmit</verify>
  <done>Routing logic and email stubs are implemented.</done>
</task>

<task type="auto">
  <name>Update Leave Endpoints</name>
  <files>Backend/src/routes/employeeManagement.ts</files>
  <action>
    - Refactor `POST /leaves` to use `determineLeaveRoutingRole` from `leaveRouting.ts` and call the email service stub to notify the approver.
    - Add `GET /leaves/pending` endpoint accepting `?role=` query param to return leaves routed to that role, or all pending leaves if the user is an Admin.
    - Refactor `GET /leaves` to accept `?employeeId=` to return "own leaves".
    - Refactor `PUT /leaves/:id/status` to trigger the email service to notify the applicant on approval/rejection.
  </action>
  <verify>tsc --noEmit</verify>
  <done>Endpoints are fully functional and compile successfully.</done>
</task>

## Success Criteria
- [ ] Leave routing logic perfectly mirrors the R7 hierarchy.
- [ ] Email notifications are sent (or logged) on application and status change.
- [ ] Endpoints support filtering by employee and pending role.
